#include "main.h"

/**
 * main - Entry point for testing the positive_or_negative function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
